{
  "name": "solace7-backend",
  "version": "2.0.0",
  "description": "Backend for SOLACE-7 — real accounts on Postgres, plus a proxy for Groq and Tavily so no API key is ever exposed to the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}
